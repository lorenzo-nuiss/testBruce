{
  "name": "backendtest",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf out; tsc -p tsconfig.json",
    "verify": "rm -rf out; tsc -p tsconfig.json; jest out/test/check",
    "winbuild": "rmdir out; tsc -p tsconfig.json",
    "winverify": "rmdir out; tsc -p tsconfig.json; jest out/test/check",
    "lint": "tslint --project .",
    "fullverify": "rm -rf out; tsc -p tsconfig.json; jest out/test/check; tslint --project ."
  },
  "author": "",
  "license": "",
  "dependencies": {
    "@types/jest": "^29.2.3",
    "jest": "^29.3.1",
    "tslint": "^6.1.3",
    "typescript": "^4.9.3"
  }
}